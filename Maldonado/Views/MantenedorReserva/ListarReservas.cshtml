@model IEnumerable<CapaEntidad.entReserva>

@{
    ViewBag.Title = "Gestionar Reservas de Clientes";
    ViewBag.image = "/Contents/images/security.png";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="display-4 ">Lista de Reservas del Hotel</h2>

<p>
    @Html.ActionLink("Nueva Reserva", "InsertarReserva")
</p>
<table class="table sortable">
    <thead class="thead-dark">
        <tr>
            <th scope="col" onclick="sortTable(0, 'str')">Id Reserva</th>
            <th scope="col" onclick="sortTable(1, 'str')">Nombre</th>
            <th scope="col" onclick="sortTable(2, 'str')">Apellido</th>
            <th scope="col" onclick="sortTable(3, 'int')">N° de Habitacion</th>
            <th scope="col" onclick="sortTable(4, 'str')">Ubicacion</th>
            <th scope="col" onclick="sortTable(5, 'str')">Descripcion Tipo Habitacion</th>
            <th scope="col" onclick="sortTable(6, 'str')">Fecha de Inicio</th>
            <th scope="col" onclick="sortTable(7, 'str')">Fecha de Fin</th>
            <th scope="col">Estado</th>
            <th scope="col">Opciones</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">
                    @Html.DisplayFor(modelItem => item.idReserva)
                </th>
                <td>
                    @Html.DisplayFor(modelItem => item.idCliente.nombreCliente)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.idCliente.apellidoCliente)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.idHabitacion.numeroHabitacion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.idHabitacion.descHabitacion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.idHabitacion.idTipoHabitacion.desTipoHabitacion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.fechaIncioReserva)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.fechaFinReserva)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EstReserva)
                </td>
                <td>
                    @Html.ActionLink("Eliminar", "EliminarReserva", new { idReserva = @item.idReserva })
                </td>
            </tr>
        }

    </tbody>

</table>

<a href="@Url.Action("Index","MenuIntranet")">
    <h4>Volver al Panel de Gestion</h4>
</a>

@Scripts.Render("~/bundles/OrdenarTablas")
